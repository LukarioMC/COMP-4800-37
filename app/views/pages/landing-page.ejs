<%- include('../partials/header'); %> <%- include('../partials/navbar'); %>

<main class="container-xl">
    <div class="jumbotron text-center mb-5">
        <h1 class="p-5">Welcome to 37 Heaven!</h1>
        <div class="accordion" id="fact-of-the-day">
        <% if (typeof factoid !== 'undefined') { %>
            <%- include('../partials/fact.ejs', { factoid, accordionParent:
            '#fact-of-the-day' }); %>
        <% } %>
        </div>
    </div>
    <div class="input-group">
        <input class="form-control" type="text" placeholder="Look up a fact by text..." />
        <button class="btn btn-outline-secondary" type="button" id="search-by-text-btn">
            Search
        </button>
    </div>
</main>

<%- include('../partials/footer'); %>